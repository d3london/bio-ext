# BRCA Classifier

## Objectives
[](1) Download pre-trained BERT type model from HuggingFace
[](2) Train / test / valid split on set of .jsons
[](3) Set up training parameters
[](4) MLflow Experiment tracking
[](5) log fine tuned model (e.g. using mlflow.transformers.log_model)

## Questions

quick question: the pre-trained bert model weights usually is saved to root ~/.cache is this ok?

and where do we want to save the trained model?